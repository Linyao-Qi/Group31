# Version2 版本概览

---

## 版本来源

| 来源 | 成员 | 贡献内容 |
|---|---|---|
| `Group31-main_version1/` | 成员1 | TA 岗位浏览 UI、JobService 过滤排序 |
| `V1.0/` | 成员4 | MO 发布岗位、录用申请、FileUtil 序列化 |
| `Group31-main_version2/` | 成员2 | 融合以上两者，新增 TA 申请全流程 |

---

## 功能概览

### TA 端
- 浏览岗位列表，按关键词/课程/工作类型筛选，按截止日期或标题排序
- 对某个岗位发起申请，选择 CV 文件（可选）
- 查看自己所有申请的状态（颜色标注：绿=待审、蓝=录用、红=拒绝、灰=已撤回）
- 撤回"待审核"状态的申请；支持撤回并改投

### MO 端
- 动态发布新岗位（实时写入，TA 端可见）
- 对 TA 的申请进行录用操作，含权限校验（只能操作自己的岗位）

---

## 数据存储

| 文件 | 类型 | 内容 | 是否提交 Git |
|---|---|---|---|
| `data/jobs.csv` | CSV | 预置岗位（含 moId 归属列） | ✅ 提交 |
| `data/ta_profiles.csv` | CSV | TA 档案 | ✅ 提交 |
| `data/job.dat` | 二进制序列化 | MO 运行时发布的岗位 | ❌ 不提交 |
| `data/app.dat` | 二进制序列化 | 所有申请记录 | ❌ 不提交 |
| `data/profile.dat` | 二进制序列化 | TA 档案缓存 | ❌ 不提交 |
| `data/cvs/{taId}/*.pdf` | PDF | TA 上传的 CV 文件 | ❌ 不提交 |

岗位数据采用**双数据源**设计：`jobs.csv`（静态预置）+ `job.dat`（MO动态发布），查询时两者合并。

---

## 申请状态规范

| 状态 | 存储字符串 | 颜色 |
|---|---|---|
| 已提交待审 | `"Submitted"` | 绿色 |
| 已撤回 | `"Withdrawn"` | 灰色 |
| 已录用 | `"Accepted"` | 蓝色 |
| 已拒绝 | `"Rejected"` | 红色 |

**所有代码中的状态字符串必须与上表完全一致（区分大小写）。**

---

## 包结构

```
src/com/tajobsystem/
├── model/            Job, Application, TAProfile
├── data/             FileUtil, JobDataLoader, TAProfileLoader
├── service/          JobService, ApplicationService, MoService
├── ui/               TAJobsFrame, JobDetailsFrame, ApplyFrame, ApplicationStatusFrame
├── TestApplyMain.java    TA 申请流程测试入口
└── MoServiceTest.java    MO 发布/录用流程测试入口
```

---

## 当前待完善项

1. **成员1**：`TAJobsFrame` 目前只加载 `jobs.csv`，需同时加载 `job.dat`，MO 动态发布的岗位才能在 TA 浏览列表中显示。
   ```java
   // 在 TAJobsFrame 的加载逻辑中追加：
   jobs.addAll(JobDataLoader.loadJobsFromDat("data/job.dat"));
   ```

2. **成员4**：`MoService` 目前只有 `acceptApplicant()`，缺少 `rejectApplicant()` 方法。`ApplicationStatusFrame` 已支持"Rejected"红色显示，只需在 MoService 中参照 `acceptApplicant` 增加该方法，将状态设为 `"Rejected"` 即可。
