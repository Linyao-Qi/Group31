# Version2 版本概览

---

## 版本来源

| 来源 | 成员 | 贡献内容 |
|---|---|---|
| `Group31-main_version1/` | 成员1 | TA 岗位浏览 UI、JobService 过滤排序 |
| `V1.0/` | 成员4 | MO 发布岗位、录用申请 |
| `Group31-main_version2/` | 成员2 | 融合以上两者，新增 TA 申请全流程，统一数据存储格式为 CSV |

---

## 功能概览

### TA 端
- 浏览岗位列表，按关键词/课程/工作类型筛选，按截止日期或标题排序
- 对某个岗位发起申请，选择 CV 文件（可选）
- 查看自己所有申请的状态（颜色标注：绿=待审、蓝=录用、红=拒绝、灰=已撤回）
- 撤回"待审核"状态的申请；支持撤回并改投

### MO 端
- 动态发布新岗位（直接追加到 jobs.csv，TA 端立即可见）
- 对 TA 的申请进行录用操作，含权限校验（只能操作自己的岗位）

---

## 数据存储

所有数据统一以 **CSV 文本格式**存储，不再使用二进制序列化。

| 文件 | 类型 | 内容 | 是否提交 Git |
|---|---|---|---|
| `data/jobs.csv` | CSV（14列）| 所有岗位：预置岗位 + MO 运行时发布的岗位 | ✅ 提交 |
| `data/ta_profiles.csv` | CSV（4列）| TA 档案库，程序只读 | ✅ 提交 |
| `data/app.csv` | CSV（5列）| 所有申请记录，运行时生成 | ❌ 不提交 |
| `data/profile.csv` | CSV（4列）| 曾申请过的 TA 档案缓存，运行时生成 | ❌ 不提交 |
| `data/cvs/{taId}/*.pdf` | PDF | TA 上传的 CV 文件 | ❌ 不提交 |

> 详细列定义见 `data/DATA_FILES.md`。

**岗位单一数据源设计：** MO 发布的新岗位直接追加写入 `jobs.csv`，全项目读取岗位只需一个文件，无需合并多源。

---

## 申请状态规范

| 状态 | 存储字符串 | UI 颜色 |
|---|---|---|
| 已提交待审 | `"Submitted"` | 绿色 |
| 已撤回 | `"Withdrawn"` | 灰色 |
| 已录用 | `"Accepted"` | 蓝色 |
| 已拒绝 | `"Rejected"` | 红色 |

**所有代码中的状态字符串必须与上表完全一致（区分大小写）。**

---

## 包结构

```
src/com/tajobsystem/
├── model/            Job, Application, TAProfile
├── data/             CsvUtil, JobDataLoader, TAProfileLoader, ApplicationLoader
├── service/          JobService, ApplicationService, MoService
├── ui/               TAJobsFrame, JobDetailsFrame, ApplyFrame, ApplicationStatusFrame
├── TestApplyMain.java    TA 申请流程测试入口
└── MoServiceTest.java    MO 发布/录用流程测试入口
```

---

## 当前待完善项

1. **成员4**：`MoService` 目前只有 `acceptApplicant()`，缺少 `rejectApplicant()` 方法。`ApplicationStatusFrame` 已支持 `"Rejected"` 红色显示，只需在 `MoService` 中参照 `acceptApplicant` 增加该方法，将状态设为 `"Rejected"` 即可。

2. **成员4**：MO 端 UI 展示时应将存储值 `"Submitted"` 显示为 `"Pending"`，以符合 MO 视角下"待处理"的语义。转换逻辑放在 MO 端的表格渲染处，不需要改动数据存储。
