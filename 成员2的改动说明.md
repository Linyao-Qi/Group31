# 成员2对已有代码的改动说明

本文档说明成员2在融合版本（version2）中对成员1、成员4代码所做的修改及原因。

---

## 对成员1代码的改动

### 1. `model/Job.java` — 新增两个字段

**改动：** 在 Job 模型中新增 `moId`（负责该岗位的 MO ID）和 `jobStatus`（岗位状态）两个字段，并添加对应 setter。原有12参数构造函数不变。

**原因：** MO 发布岗位时需要记录是谁发布的（moId），以便后续录用时进行权限校验（"这个 MO 只能操作自己发布的岗位"）。`jobs.csv` 中的预置岗位同样需要 moId 字段，才能让 MO 对预置岗位的申请也进行正常处理。

---

### 2. `data/JobDataLoader.java` — 支持读取第13列 moId，新增 loadJobsFromDat 方法

**改动1：** CSV 解析时，若存在第13列则读取为 moId：
```java
if (parts.length >= 13 && !parts[12].isBlank()) {
    job.setMoId(parts[12].trim());
}
```

**改动2：** 新增静态方法：
```java
public static List<Job> loadJobsFromDat(String filePath) {
    return FileUtil.read(filePath);
}
```

**原因：** 改动1配合 jobs.csv 新增的 moId 列使用。改动2是为了在 TestApplyMain 和后续 TAJobsFrame 中能统一调用一个方法加载 .dat 岗位，无需直接调用 FileUtil。

---

### 3. `data/jobs.csv` — 新增第13列 moId

**改动：** 在原有12列末尾追加 moId 列，J001/J002/J005 归属 MO001，J003/J004/J006 归属 MO002。

**原因：** MoService 的 `acceptApplicant()` 需要校验"申请对应的岗位是否属于当前 MO"。预置岗位原来没有 moId，导致权限校验始终失败，MO 无法录用任何预置岗位的申请人。

---

## 对成员4代码的改动

### 1. `service/MoService.java` — 状态字符串统一

**改动：**
```java
// 原来
if (!"PENDING".equals(target.getAppStatus())) { ... }
target.setAppStatus("ACCEPTED");

// 修改后
if (!"Submitted".equals(target.getAppStatus())) { ... }
target.setAppStatus("Accepted");
```

**原因：** 成员2的 `ApplicationService.applyForJob()` 写入的状态是 `"Submitted"`，UI 层 `ApplicationStatusFrame` 的颜色渲染也只识别 `"Accepted"`（首字母大写）。原来用全大写 `"PENDING"` / `"ACCEPTED"` 会导致：TA 提交申请后 MO 调用 `acceptApplicant()` 始终报"只能处理待审核申请"；录用后状态也无法显示为蓝色。

---

### 2. `service/MoService.java` — acceptApplicant 同时搜索双数据源

**改动：**
```java
// 原来只搜索 job.dat
List<Job> jobs = FileUtil.read(JOB_FILE);

// 修改后同时搜索 job.dat + jobs.csv
List<Job> allJobs = new ArrayList<>(FileUtil.read(JOB_FILE));
allJobs.addAll(JobDataLoader.loadJobsFromCSV(JOB_CSV));
```

**原因：** TA 申请的岗位来自 `jobs.csv`（预置岗位），而原来 `acceptApplicant()` 只在 `job.dat`（MO动态发布的岗位）中查找，导致找不到岗位、MO 无法录用任何预置岗位的申请人。

---

### 3. `MoServiceTest.java` — 迁移到包结构下，硬编码状态改为 "Submitted"

**改动：** 将 `V1.0/MoServiceTest.java` 迁移至 `src/com/tajobsystem/MoServiceTest.java`，添加包声明和 import，同时将测试数据中硬编码的申请状态从 `"PENDING"` 改为 `"Submitted"`。

**原因：** 原文件在 V1.0 扁平目录结构下，无法在 version2 的包结构中编译运行。状态字符串同步修改是为了让测试数据与实际 ApplicationService 写入的状态一致，否则 acceptApplicant 测试永远失败。
